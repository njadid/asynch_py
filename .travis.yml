language: c
compiler:
 - gcc
 - clang
addons:
  apt:
    packages:
     - check
     - libopenmpi-dev
     - openmpi-bin
     - libpq-dev
script:
 - mkdir -v build
 - cd build
 - autoreconf -ivf ..
 - ../configure --prefix=${HOME}
 - make check
